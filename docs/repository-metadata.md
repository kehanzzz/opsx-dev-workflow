# Repository Metadata / 仓库元数据

## GitHub Description / 仓库描述

Recommended:

`A workflow skill that turns OpenSpec and superpowers into a structured end-to-end delivery flow.`

建议文案：

`把 OpenSpec 和 superpowers 串成结构化端到端交付流程的 workflow skill。`

## Short Pitch / 一句话介绍

Recommended:

`Explore, spec, plan, execute, verify, archive, finish. One workflow, explicit gates.`

建议文案：

`从探索到归档，再到分支收尾。一个工作流，明确的阶段门。`

## About Section / About 区域

Suggested highlights:

- Workflow skill, not a vendored toolkit
- Uses upstream OpenSpec and superpowers directly
- Keeps change state and audit trail explicit
- Supports Codex, Claude Code, Gemini CLI, and opencode

建议突出这 4 点：

- 它是 workflow skill，不是上游工具包镜像
- 直接依赖上游 OpenSpec 和 superpowers
- 强调状态记录和审计链路
- 面向 Codex、Claude Code、Gemini CLI、opencode

## Suggested Topics / 推荐 Topics

Recommended set:

- `ai-agent`
- `agent-workflow`
- `developer-tools`
- `openspec`
- `superpowers`
- `codex`
- `claude-code`
- `gemini-cli`
- `opencode`
- `workflow-skill`
- `spec-driven-development`
- `multi-agent`

建议保留 8 到 12 个，不要堆太满。

## Social Preview Copy / 社交预览文案

Recommended:

`OpsX Development Workflow helps agent-driven work stop feeling ad hoc. It adds explicit gates for explore, spec, plan, execute, verify, archive, and branch finish, while keeping OpenSpec and superpowers upstream.`

建议文案：

`OpsX Development Workflow 让 agent 协作不再像临时拼接。它为探索、规格、计划、执行、验证、归档和分支收尾建立明确阶段，同时保持 OpenSpec 和 superpowers 依赖外置。`

## Naming Rule / 命名规则

- Public repo name: `opsx-development-workflow`
- Published install unit: `skill/`
- Skill name: `opsx-development-workflow`
- Upstream OpenSpec references: use current `openspec-*` skill names

对外命名尽量收敛，不要混用旧的 `/opsx:*` 叫法当主路径。
